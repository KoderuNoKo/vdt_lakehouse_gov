# pii_classification.detectors — column-name and regex PII detectors

from pii_classification.detectors.column_name_detector import ColumnNameDetector
from pii_classification.detectors.regex_detector import RegexDetector
